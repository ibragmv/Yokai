{
  "name": "yokai",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "start": "bun run --cwd apps/web start",
    "build": "bun run --cwd apps/web build",
    "lint": "bun run --cwd apps/web lint",
    "types": "bun run --cwd apps/web types",
    "format": "biome format --write .",
    "check": "bun run lint && bun run types",
    "convex": "bun run --cwd apps/web scripts/convex-command.ts dev watch",
    "sync": "bun run --cwd apps/web scripts/convex-command.ts dev sync",
    "deploy": "bun run --cwd apps/web scripts/convex-command.ts prod deploy",
    "logs": "bun run --cwd apps/web scripts/convex-command.ts prod logs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}

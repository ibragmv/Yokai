{
  "name": "web",
  "private": true,
  "scripts": {
    "dev": "bun run next:dev",
    "next:dev": "next dev --turbopack",
    "convex:dev": "bunx convex dev --env-file .env",
    "convex:sync:dev": "bunx convex dev --once --env-file .env",
    "convex:deploy": "bunx convex deploy",
    "convex:logs:dev": "bunx convex logs",
    "convex:logs:prod": "bunx convex logs --prod",
    "build": "next build",
    "start": "next start",
    "lint": "biome check src convex/*.ts next.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/sandbox": "^1.8.0",
    "convex": "^1.34.1",
    "next": "^16.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "typescript": "^5.8.2"
  }
}
